# 存档系统说明

魂类"火堆存档 + 死亡回滚 + 永久进度"的存档系统。一句话:**存档 = 把「全局态(玩家/背包/装备/技能/钥匙)+ 当前世界态(怪/门/箱/拾取/商店)+ 复活点」快照进文件;读档 = 整场景重建 + 从快照逐项覆盖运行时态,严格复刻,不残留"读档前到处乱搞"的痕迹。**

相关文件:
- `Assets/Scripts/Systems/SaveSystem.cs` — 核心:数据模型、读写、apply 流水线、运行时集合
- `Assets/Scripts/Systems/SaveIdUtility.cs` — 稳定 SaveID(资产 ID / 场景物体 ID,带场景前缀)
- `Assets/Scripts/Systems/CheckpointManager.cs` — 火堆检查点(存 + 复活点)
- `Assets/Scripts/GameManager.cs` — 死亡重生 / 场景切换入口(教程死亡走 `RespawnInPlace`)
- `Assets/Scripts/Systems/LevelManager.cs` — boss 击破后完整 autosave
- 受存档驱动的对象:`EnemyBase`(`LoadSaveState`)、`LockedDoor`/`ChestInteract`/`TutorialGate`(`LoadOpened`)、`ItemPickup`/`EquipmentPickup`(`SyncToInventory`/`SyncToEquipment`)、`ShopSystem`(`LoadSaveData`)、`LevelKeyManager`、`BossIntroTrigger`/`VictoryGate`/`EnemyClearGate`(订阅 `AfterApply`)

---

## 一、存什么 + 存哪

文件(`%LocalLow%/DefaultCompany/Unfulfilled Wish/`):
- `save_auto.json` — **唯一自动档**(检查点 / 死亡重生 / 主菜单继续 都读它)
- `save_0/1/2.json` + 同名 `.png` 缩略图 — 3 个**手动槽**(ESC 菜单)

`BuildSaveData` 快照内容:
| 类别 | 字段 |
|---|---|
| 玩家 | 裸 base 三围(攻/防/最大血,**扣掉装备加成存裸值**)、当前血/虚血/体力、金币、`playerX/Y`(读档落点)、`respawnX/Y`(火堆复活点) |
| 持有 | 背包物品、拥有装备 + 4 个已装槽、已学技能、钥匙(计数 + ID 列表) |
| 世界 | `enemyStates`(**只存 permadeath 怪**)、`doorStates`(开过的门/箱/教程门)、`shopStates`(限量售罄) |
| 进度 | 解锁检查点 + 最后检查点 ID、场景名、存档时间 |

**ID 规则**(`SaveIdUtility`):
- **资产**(物品/装备/技能 ScriptableObject)→ 资产 ID,**不带场景**(全局通用);
- **场景物体**(怪/门/箱)→ `场景名:层级路径` 前缀,**防跨场景撞 ID**。

## 二、什么时候存(自动档只在 4 处写)
1. **坐火堆**(`CheckpointManager.ActivateCheckpoint → Save`):落点 = 复活点 = 火堆;boss 战中被拦截不写;
2. **新游戏首存**(进首关后 `AutoSaveAtPlayer`):让"继续"立刻可用、退出不丢新局;
3. **手动读档后镜像**(`LoadSlot` 里 `WriteSlot(AutoSlot, 所读档)`):复活点跟着所读的档走;
4. **击破 boss**(`LevelManager.OnBossDefeated → AutoSaveAtPlayer`):把"boss 已死 + 当前背包/装备/金币"**原子写**进自动档,杜绝"boss 死了但掉落没了"的 desync。

> 另:杀 permadeath 怪时 `PersistEnemyDefeat` 会**就地改写 save_auto 的 enemyStates**(只补这一个死亡标记,不带背包)。
> **手动槽**由 ESC 菜单 `SaveToSlot` 随时写(抓当前位置 + 全套态 + 截图;复活点沿用自动档里的火堆点)。

## 三、读档三条路径
| 路径 | 触发 | 流程 |
|---|---|---|
| **死亡重生** | 非教程场景死亡 | `PrepareRespawn`(置重生标志 + `globalStateLoaded=false`)→ 重载存档场景 → 读 save_auto,落"火堆复活点"、**满血** |
| **主菜单继续** | 主菜单 Continue | `RequestFullReload` → 重载存档场景 → 读 save_auto,落"读档落点" |
| **手动读档** | ESC → 读某槽 | `LoadSlot`:**先镜像进 save_auto** → 跨场景/教程 整场景重载;否则原地 `ApplySave` |

> **教程是特例**:教程内死亡 = **原地复活**(`RespawnInPlace`,不读档、保留进度);教程读档则**强制整场景重载**(绕开 `TutorialSequence` 不进存档的运行时态,见 §七)。

## 四、读档恢复流水线
```
真读档(继续 / 重生 / 手动读档)= globalStateLoaded 尚未确立 → realLoad=true
  ├ ApplyGlobalState   玩家属性/背包/装备/技能/钥匙/血量(LoadXxx 清空重建)
  └ ApplySceneState
      ├ RefreshRespawnableEnemies   复活普通怪
      ├ ApplyEnemyStates            清 runtimeDefeated → 从档重建 → EnemyBase.LoadSaveState(双向)
      ├ ApplyDoorStates(realLoad)   真读档:清 runtimeOpened → 从档重建;门/箱/教程门统一 LoadOpened(双向)
      ├ 拾取物 Sync*(双向,含 inactive)  背包有=隐藏,没有=重新出现在地上
      ├ ApplyShopStates
      └ AfterApply 事件             boss 重新沉睡 / 雾门·通关门按场上是否有活 boss 自洽
普通过门切场景 = globalStateLoaded 已确立 → realLoad=false → 全局态 + 门集合都【不重建】,内存连续
```

## 五、关键机制
- **内存连续(门 + 背包同款)**:`runtimeOpenedDoorIDs` 和背包一样,**只在真读档时从存档重建,普通过场景保留内存** → 开过的门/箱过场景不丢、回来不重置(**堵死宝箱重复刷**);唯有死亡/读档才按存档回滚。三类门(`LockedDoor`/`ChestInteract`/`TutorialGate`)统一读这个集合,才能整体内存连续。
- **运行时集合是从存档重建的**:`runtimeDefeated` / `runtimeOpened` 真读档时清空 + 从档重建 → **存档是权威**,读档前的运行时残留被覆盖,**无跨场景泄漏**。
- **落点 vs 复活点**:正常读档/继续用 `playerX/Y`(上次所在);死亡重生用 `respawnX/Y`(火堆)、并满血。
- **双向恢复**:门/箱/教程门/拾取的 `LoadXxx`/`Sync*` 都"开也管、关也管" → 原地读旧档能正确回退(不会"门关不回来""拾取凭空消失")。
- **资产解析走 Resources**:`Resources.LoadAll<T>("Data")`(缓存常驻)按 saveID 还原背包/装备 → **打包版也不丢持有物**(不依赖编辑器内存里的资产)。

## 六、各类对象的存档规则
| 对象 | 规则 |
|---|---|
| **permadeath 怪(boss)** | 击杀永久死;杀那刻即时写自动档 + 击破后完整 autosave;死亡/继续保持死 |
| **普通怪** | 不进存档,任何读档 / 进场景都复活(魂式刷怪) |
| **门 / 宝箱 / 教程门** | 开过的进存档;内存连续(过场景不丢),真读档按存档回滚 |
| **地上拾取(道具 / 装备)** | 不单独存;按读回来的背包双向显隐(背包有=隐,没有=出现在地上) |
| **钥匙** | 拾取后存(计数 + ID) |
| **商店限量品** | 售罄状态存 |
| **背包 / 装备 / 技能 / 金币 / 血量** | 全局态,真读档清空重建 |

## 七、能不能"完全复刻存档时的场景"
- **物件类(门 / 箱 / 地上道具)+ 玩家持有(背包 / 装备 / 钱 / 钥匙)= 100% 复刻,零跨场景泄漏**——不管读档前在别的场景开了多少箱、捡了多少东西,真读档都清空重建。
- **两条有意偏差**(都在"杀怪"上):
  1. **普通怪必复活**——非 permadeath 怪不存死亡,任何读档都刷新;
  2. **被杀 boss 在自动档里保持死**——`PersistEnemyDefeat` 即时写 save_auto,死亡/继续读到的是"boss 已死",不回滚到打它之前(魂式"杀过的 boss 不重打")。手动档是固定快照,不受此影响。
- **教程引导进度**不进存档,但**进度其实编码在"哪些门开着"**(门进存档)。教程整重载后,`TutorialSequence` 组件虽重置,但**门已开的教学站会自动判完成跳过**(`TutorialSequence.cs` 的 `gate.IsOpen → finished`)→ 接着当前进度走,不卡;只有"主菜单继续"才从教程开头(因为教程的 save_auto 一直停在新游戏首存)。
