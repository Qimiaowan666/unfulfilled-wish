# 敌人攻击系统说明

小怪与 boss **共用同一套**数据驱动攻击系统。核心理念:**招只管"打什么",连段管"怎么串、何时放",每一段用两个可插拔槽管"怎么到位、怎么打"**。配新攻击基本不用写代码,在 Inspector 下拉 + 填参数即可;任何怪都能复用所有位移/编排。

相关文件:
- `Assets/Scripts/Enemy/EnemyBase.cs` — 数据模型(HitProfile / EnemyAttack / ComboStep / EnemyCombo)+ 抽签逻辑
- `Assets/Scripts/Enemy/AttackRunner.cs` — 运行器(执行一招/一套连段)
- `Assets/Scripts/Enemy/StepMovers.cs` — 出招前位移(StepMover 及子类)
- `Assets/Scripts/Enemy/AttackDrivers.cs` — 出招时编排(AttackDriverBase 及子类)
- `Assets/Scripts/Editor/SubclassSelectorDrawer.cs` — 下拉选类型的 Inspector drawer
- `Assets/Scripts/Editor/MinMaxSliderDrawer.cs` — 双手柄区间滑块(range / hpPercent 用)

---

## 一、数据三层

### 1. 招 EnemyAttack(`attacks[]`)——纯打击数据,"打什么"
| 字段 | 含义 |
|---|---|
| `id` | 唯一键。连段按它引用;**同时必须是 Animator 里的状态名**(出招会强播这个 clip) |
| `hits[]` | 这招打出的每一下(`HitProfile`)。动画事件 `Fire(i)` 引用第 i 下 |
| `showGizmo` | 是否画命中盒 gizmo(调试) |

**HitProfile(一下命中,自包含):**
| 字段 | 含义 |
|---|---|
| `delivery` | `Melee`(近战命中盒)/ `Ranged`(发射箭矢) |
| `red` | 红=不可格挡 + 红色视觉;白=可格挡 |
| `damageMultiplier` | 伤害 = 敌人 `attack` × 此值 |
| `knockback` / `hitStun` | 击退力 / 命中硬直 |
| `hitboxOffset` / `hitboxSize` | 近战命中盒(相对身体,自动按朝向翻转) |
| `projectilePrefab` | 远程(Ranged)发射的箭 |

> 招**不含**任何位移/节奏/动画速度——那些全在连段的段上。所以同一招能被不同段以不同方式使用。

### 2. 连段 EnemyCombo(`combos[]`)——"怎么串、何时放"
一只怪可配多套连段,出招时按**权重 + 距离 + 血量**抽一套。
| 字段 | 含义 |
|---|---|
| `name` | 名字(仅标识) |
| `weight` | 抽签权重。`0` = 不随机抽(通常配合强制开场用) |
| `range` (Vector2 滑块) | 可被抽中的**距离区间**(米)。玩家在 [min,max] 内才可能抽到 |
| `hpPercent` (Vector2 滑块) | 可被抽中的**血量区间**(0~1)。配低血段 → 残血才放,不必写阶段状态机 |
| `noRepeat` | 不连续抽到同一套(增加变化)。**软偏好**:排除上一套后若无招可放(如某范围只有这一套),仍会放它 → 不会卡住哑火 |
| `cooldownAfter` | 整套打完的冷却(秒);≤0 用敌人默认 `attackCooldown` |
| `stepGap` | 段间默认停顿(秒),0 = 无缝;每段可用自己的 `gap` 覆盖 |
| `steps[]` | 这套连段的每一段(`ComboStep`) |

> 怪的"决策距离"`MaxComboRange()` = 所有连段 `range.y` 的最大值(没有专门的 attackRange 字段)。

### 3. 一段 ComboStep(`steps[]`)——"打哪招 + 怎么到位 + 怎么打"
| 字段 | 含义 |
|---|---|
| `attackId` | 打哪招(引用 `attacks[].id`) |
| **`preMove`**(出招前) | **段前位移**,可插拔 `StepMover`;留空=不动。详见 §2 |
| **`driver`**(出招时) | **出招编排**,可插拔 `AttackDriverBase`;留空=普通挥砍。详见 §3 |
| `animSpeed` | 动画播放速度;≤0 = 用 1 |
| `gap` | 本段打完后的停顿(秒);<0 = 用连段的 `stepGap` |

---

## 二、出招前 `preMove`(StepMover)——把敌人挪到位,**到位了才挥砍**

出招**之前**的定位,跑完才进入挥砍。Inspector 下拉选类型,选中后展开它自带的参数。

| 选项 | 类 | 参数 | 行为 |
|---|---|---|---|
| **逼近 (走近)** | `ApproachMover` | `reach`/`maxTime` | 朝玩家走到 `reach`(命中距离)内就停(或遇崖/超时)。`reach` 是自己的停止距离,不再借连段的选取范围 |
| **后撤 (走开)** | `RetreatMover` | `maxTime` | 背对玩家走,遇崖/超时停 |
| **瞬移 (闪身)** | `TeleportMover` | `side`/`offset`/`duration`/`clearance`/`landing`/`wallLayer` | 淡出→闪到玩家身后/身前/另一侧(贴墙修正)→淡入→硬直。腾空无敌 |
| **跳接近 (先跳后砍)** | `JumpMover` | `height`/`time`/`standoff` | 抛物线跳到玩家身位,**落地后再起手砍**。腾空无敌 |

**TeleportMover 参数:** `side`(Behind 身后 / Front 身前 / OtherSide 另一侧)、`offset` 距玩家多远、`duration` 淡出+淡入总时长、`clearance` 距墙安全距离、`landing` 现身后到出招的硬直、`wallLayer` 墙体检测层(留空自动用 Ground)。

---

## 三、出招时 `driver`(AttackDriverBase)——挥砍**过程中**的位移 + 命中编排

留空 = 普通挥砍(`Fire(i)` 立即打 `hits[i]`)。有驱动则挥砍期间每帧跑 `Tick`,命中事件转给驱动。

| 选项 | 类 | 参数 | 行为 |
|---|---|---|---|
| **挑飞 (横劈→砸)** | `LaunchDriver` | `riseHeight`/`launchTime`/`bossJumpDelay`/`slamTime`/`slamMult`/`holdDistance` | Fire(0) 横劈挑起玩家→boss 延迟追上→Fire(1) 一起砸地(伤害 ×slamMult)。不锁定:空中可翻滚/格挡挣脱 |
| **跳劈 (边跳边砍)** | `JumpDriver` | `height`/`time`/`standoff` | 抛物线跳向玩家,**攻击 clip 与跳同时进行**,落地劈中。腾空无敌、落地解。`time` 应 ≤ clip 时长 |
| **前冲** | `LungeForward` | `speed` | 挥砍中朝玩家前冲(离玩家 >1.5 才冲) |
| **后撤** | `LungeBackward` | `speed` | 挥砍中背身后撤(遇崖停) |

> **跳接近(JumpMover,出招前)vs 跳劈(JumpDriver,出招时)**:前者"先跳到位、落地再起手",纯接近用;后者"跳和砍同时、落地劈中",是跳劈的正确形态。

---

## 四、运行器 AttackRunner 流程

`EnemyBase` 持有一个 `AttackRunner`(`Attack` 属性),小怪状态机和 boss 状态机都调它的 `Begin/Tick/Active/Cancel`。

```
Begin → 取当前段
  │
  ├─ PreMove 相位:有 preMove 就 Begin+每帧 Tick,返回 true(到位)才继续
  │
  ├─ Swing 相位:强播 attackId 的 clip + 点亮 isAttacking;有 driver 则 Begin+每帧 Tick
  │     命中:Fire(i) →(有 driver 转 driver.OnFire / 否则 e.DoFireHit(i))
  │     退出:动画事件 AnimationTrigger/AnimFinish,或超时(clip 时长 + 余量)
  │
  ├─ Gap 相位:段后停顿(step.gap / combo.stepGap)
  │
  └─ 推进连段:还有下一段 → 回 PreMove;打完 → 进冷却,回 Idle
```

被打断(破韧/死亡/识破)调 `Cancel()`:`mover.Cancel` + `driver.End` 恢复物理/无敌/可见,重置连段。

**动画事件约定(挂在攻击 clip 上):**
- `Fire(int i)` — 第 i 下命中生效
- `AnimationTrigger` / `AnimFinish` — clip 放完,推进连段 / 退出
- `Warn` / `WarnEnd` — 预警视觉(红光起/灭),用于"即将出招"提示

---

## 五、怎么用

### 配一个新攻击(零代码)
1. 在怪的 `attacks[]` 加一招:填 `id`(=Animator 状态名)+ `hits[]`(命中盒/伤害);
2. 在 `combos[]` 加一套连段:设 `range`/`hpPercent`/`weight`,加 `steps`;
3. 每段:选 `attackId`,需要的话 **出招前**下拉选位移、**出招时**下拉选编排,填参数;
4. 给那招的 clip 加 `Fire(i)` + `AnimFinish` 动画事件。

### 复用到别的怪
所有 Mover/Driver 的参数都在**段实例上**(不在 EnemyBase / 某个子类),且只调 EnemyBase 公共 API。**任何怪挂上就能用**——比如给小怪某段的"出招前"选 `瞬移`,它就会闪身偷袭,零代码。
> 前提:那招要播的 clip 在该怪 Animator 里存在;怪已进入战斗(锁定了 player)。

### 扩展新行为
- 新的段前位移 → 写 `XxxMover : StepMover`(实现 `Begin`/`Tick`→bool/`Cancel`);
- 新的出招编排 → 写 `XxxDriver : AttackDriverBase`(`Begin`/`Tick`/`OnFire`/`End`);

只要类标了 `[System.Serializable]`,**下拉里自动多一项,所有怪立即可用**(类型选择器 `SubclassSelectorDrawer` 自动列出所有非抽象子类;中文名在该 drawer 的 `Nice` 字典里补)。

---

## 六、当前各敌人配置一览

| 敌人 | 攻击/连段要点 |
|---|---|
| **AoTengu** | 最基础,单招,无位移/编排 |
| **Archer** | 远程放箭(Ranged hits)+ 后撤拉距 |
| **DemonSamurai** | 多招;`jumpattack` 段用 `LungeForward`(前冲)|
| **MinotaurBoss** | 跳劈接大招(`JumpDriver`)、挥飞接大招(`LaunchDriver`)、闪身斩/瞬移大招/二阶段开场(`TeleportMover`);低血/距离 gating + 强制开场 |
