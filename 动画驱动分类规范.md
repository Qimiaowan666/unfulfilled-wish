# 动画驱动分类规范

> 本项目所有角色(主角 / 小怪 / boss)的 Animator 状态,按下面的标准选择驱动方式。
> 核心理念:**没有"唯一正确",按场景用对工具**。加新状态时照此判定。

---

## 一、三种驱动方式

| 方式 | 谁说了算 | 优点 | 缺点 |
|---|---|---|---|
| **参数驱动**(bool / int) | Animator 图按参数过渡 | 可视化、不易 desync、好给策划调 | 封闭——加变体要去图里连线 |
| **代码驱动**(`Anim.Play` / `CrossFade`) | C# 强播 clip | 灵活、数据驱动、可自定义播放(倍速/起始帧) | 失去可视化、要手动管混合 |
| **Blend Tree**(float) | 按数值平滑混合 | 移动等连续过渡顺滑 | 仅适合"按值混合"的场景 |

---

## 二、判定标准(决策树)

对每个状态,依次问:

1. **需要按一个数值「平滑混合」吗?**(速度→idle↔走↔跑、瞄准角)
   → **Blend Tree(float)**。2D 像素游戏多数不需要(要脆切)。

2. **这个状态就对应「写死的一个 clip」,做的时候就知道是哪个?**
   → **bool**。进状态设 true、出状态设 false。

3. **是「一个动作的若干固定变体」,按编号/模式选?**
   → **bool(摁住)+ 选择器**:
   - 变体是**有序的几下** → **int 参数**(连段 AttackStep 1/2/3)
   - 变体是**二选一的模式** → **额外一个 bool**(isFlame:普通/火焰)

4. **clip 由「运行时数据」决定(配置给的 id/名字),或需要「自定义播放」(倍速/起始帧/强制重播)?**
   → **`Anim.Play` / `CrossFade`**。

### 核心分水岭(一句话)

> **可能的 clip 集合是「封闭已知」还是「开放/数据驱动」?**
> - **封闭、做的时候就定死** → 参数驱动(bool / int)。图干净、可视化。
> - **开放、配置说了算 / 要自定义播放** → `Anim.Play`。加东西不改图。

这就是"一致性 vs 扩展性"的取舍点。

---

## 三、本项目归类

| 类别 | 判定 | 具体状态 |
|---|---|---|
| **bool** | 写死的 1:1 状态↔clip | idle / 走跑(walk·run) / jump / fall / dash / block / counter(完美格挡) / execute(处决) / 受击(hit·hurt·stunned) / death / 治疗(rest) —— 主角 + 小怪 + boss 全部 |
| **bool + int** | 一个动作、有序几下 | **主角普攻连段**:`isAttacking` 摁住 + `AttackStep` 整数(Attack 子状态机 attack1/2/3) |
| **bool(模式)** | 一个动作、二选一模式 | **DemonSamurai 形态**:`isFlame` —— idle/run/hurt 普通版 vs `_flame` 火焰版(翻倍过渡) |
| **Anim.Play(数据驱动)** | clip 由配置 id 决定 | **小怪/boss 所有攻击招式**:`Anim.Play(招id)`(统一攻击运行器,招式是数据) |
| **Anim.Play(自定义播放)** | 特定 clip + 自定义倍速 | **主角 DashStrike**:`Anim.Play("attack2")` @4x |
| **Anim.Play(动态变体)** | 运行时按状态选 clip | **DemonSamurai 攻击的火焰变体**:`ResolveClip` 加 `_flame` 后缀 |
| **Anim.Play(协程特殊序列)** | 一次性脚本序列、无对应状态机状态 | **DemonSamurai 变身 shout**:协程里强播 + `ExitTime` 退出 |

### 关键约定

- **敌人攻击不挂 Entry 过渡**:攻击由 `Anim.Play(招id)` 强制进入、`isAttacking` 摁住、`→Exit(!isAttacking)` 退出。所有攻击一视同仁,图上不连 Entry(诚实反映"代码强播进入")。
- **主角连段挂 Entry**:`Entry→Attack 子状态机 (isAttacking)` + `AttackStepEquals1/2/3`,是**参数真驱动**(非 Anim.Play),所以有真 Entry 入口。
- **状态名 = clip 名 / 语义**:如 counter 状态播 counter clip(避免"perfect block 状态播 counter clip"这种名实不符)。

---

## 四、怎么用(加新状态时)

1. **默认先用 bool**——大多数状态都属这类;
2. 同一动作有**几个固定变体** → 升级到 bool+int(有序)或 +模式 bool(二选一);
3. 变体**开放/由数据决定**,或要**倍速/特殊播放** → 才用 `Anim.Play`;
4. 要**平滑混合** → Blend Tree。

### 反向自查(发现这两种情况就是分类错了)

- 「为了支持配置里新加的一招,得去 Animator 连一条新过渡」 → **该用 Anim.Play,误用了 bool**(开放集合套了封闭方案);
- 「一个写死的 idle 还用 Anim.Play 强播」 → **该降级成 bool**。

---

## 五、bool 与 Anim.Play 共存规则(避免冲突)

两者在同一角色/同一 Animator 里共存是常态,遵守三条就不打架:

1. **强播前先把"摁住"的 bool 设对**——先 `SetAnimBool("isAttacking")` 再 `Anim.Play("atk1")`;否则 `atk1→Exit(!isAttacking)` 当帧满足,会把刚强播进去的状态立刻弹出。
2. **用 Entry,不用 AnyState**——AnyState 每帧评估、会随时打断、和 Anim.Play 抢;Entry 只在状态机进入时评估,Anim.Play 直接绕过它,永不冲突。
3. **改了全局量(`anim.speed` / 物理 bodyType 等),Exit 里复位**——如 DashStrike 设 `anim.speed=4`,必须在 `Exit()` 复位 `=1`,否则泄漏到别的动画。

机制:`Anim.Play` 是命令式"立刻切到这"(当帧赢),bool 过渡是声明式"按 flag 留/走"(下一帧重评估)。两者一致就和平,不一致(强播进 X 但 X 的 bool 是 false)就被过渡弹走。

---

## 六、相关代码

- `Assets/Scripts/StateMachine/EntityState.cs` —— 公共状态基类,`animBoolName` 在 Enter 设 true / Exit 设 false(bool 驱动的核心)。
- `Assets/Scripts/Enemy/EnemyBase.cs` —— `SetAnimBool`(一次只亮一个 bool,跳过 isFlame);`PlayCurrentAttack`(`Anim.Play(招id)`)。
- `Assets/Scripts/Enemy/AttackRunner.cs` —— 攻击运行器:先 SetAnimBool 再 PlayCurrentAttack。
- `Assets/Scripts/Enemy/DemonSamuraiEnemy.cs` —— `ResolveClip`(攻击火焰变体)+ 变身设 `isFlame`。
- `Assets/Scripts/Player/States/Player_DashStrikeState.cs` —— Anim.Play + 自定义 `anim.speed`,Exit 复位。
