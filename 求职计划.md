# 求职计划 2026

> 目标：2026年7月1日前完成 Demo1，投递上海游戏/Unity 相关岗位
> 每天投入：约8小时
> 岗位方向：Unity 客户端程序员 / 游戏设计开发

---

## 总体时间线

```
5月9日  ─────────── 6月15日 ─────────── 7月1日 ─────────── 7月底
   Demo1 开发阶段         Demo1 收尾          开始投简历      Demo2 开发
```

---

## 第一阶段：Demo1 开发（5月9日 - 6月15日）

### 第1周（5/9 - 5/16）动画 + 角色基础

---

#### Day 1（5/9）— 切图 + 第一个动画 ✦ 今天

**上午（4小时）：手动切割 + 创建 Idle 动画**
```
1. 打开 Unity，找到 ActionSheets_V2 文件夹
2. 点击 wuxia_swordsman_idle_v2.png
   → Inspector → Sprite Mode: Multiple
   → Filter Mode: Point
   → Pixels Per Unit: 256
   → 点 Sprite Editor → Slice → Grid By Cell Size → 256×256 → Apply

3. Hierarchy 新建 GameObject → 命名 Player
   → 挂 Sprite Renderer 组件

4. 选中 Player → Ctrl+6 打开 Animation 窗口
   → 点 Create → 命名 Idle → 保存到 Assets/Animations/Player/

5. 展开 idle_v2，选前4帧拖入时间轴
   → Samples 改为 8
   → 按空格预览，确认动起来
```

**下午（3小时）：Run 动画**
```
1. 切割 wuxia_swordsman_run_v2.png（同上步骤）
2. Animation 窗口左上角下拉 → Create New Clip → 命名 Run
3. 展开 run_v2，选前6帧拖入
4. Samples 改为 12
5. 预览确认跑步循环流畅
```

**晚上（1小时）：读代码**
```
读：Assets/Scripts/Player/ 里的移动脚本
目标：说出"玩家移动是怎么实现的"这句话
```

---

#### Day 2（5/10）— Attack 动画 + Animator 状态机

**上午（4小时）：Attack1 动画 + 连接状态机**
```
1. 切割 wuxia_swordsman_attack1_v2.png
2. 创建 Attack1 动画 Clip，拖入6帧，Samples: 16
3. 双击 PlayerAnimator.controller 打开 Animator 窗口
4. 查看三个状态（Idle/Run/Attack1）是否都在
5. 添加 Parameters：
   → Bool: isRunning
   → Trigger: attack
6. 连接 Transition：
   → Idle → Run（条件：isRunning = true，取消 Has Exit Time）
   → Run → Idle（条件：isRunning = false）
   → Idle → Attack1（条件：attack Trigger）
   → Attack1 → Idle（勾选 Has Exit Time，播完回去）
```

**下午（3小时）：在代码里触发动画**
```
找到玩家移动脚本，加入 Animator 控制：
   → 移动时 SetBool("isRunning", true)
   → 静止时 SetBool("isRunning", false)
   → 按攻击键时 SetTrigger("attack")
进入 Play Mode 测试，角色能跑和攻击
```

**晚上（1小时）：读代码**
```
读：Animator 控制相关代码
目标：说出"动画状态是怎么切换的"
```

---

#### Day 3（5/11）— 修复比例 + 剩余动画

**上午（4小时）：统一 PPU + 修复显示大小**
```
1. 把所有 ActionSheets_V2 的 PPU 全改为 256
   （idle_v2 / run_v2 / attack1_v2）
2. 检查 Player 在场景里的大小
   → 应该约 1-2 Unity 单位高
   → 不合适就调整 Transform Scale（保持整数倍）
3. 确认 Point Filter Mode 已设置（不模糊）
4. 检查相机 Orthographic Size 是否合适
```

**下午（3小时）：补充缺失动画（Block / Hurt / Death）**
```
选择以下其中一种方案：
方案A：用 V1 的 block_4 / hurt_4 / death_6 先占位
       → 切割、创建 Clip、接入 Animator
方案B：用 Midjourney 生成这三个动作的 spritesheet
       → 今天生成，明天接入

建议选方案A先占位，保证流程跑通
```

**晚上（1小时）：读代码**
```
读：战斗系统伤害计算部分
目标：说出"攻击怎么造成伤害的"
```

---

#### Day 4（5/12）— 完整流程跑通

**上午（4小时）：所有状态接入 Animator**
```
添加剩余状态和 Parameters：
   → Trigger: hurt
   → Trigger: die
连接 Transition：
   → 任意状态 → Hurt（Trigger: hurt，Has Exit Time 关）
   → Hurt → 上一个状态（播完自动回）
   → 任意状态 → Death（Trigger: die）
   → Death 不返回（终态）
在代码里：
   → 受到伤害时 SetTrigger("hurt")
   → 血量归零时 SetTrigger("die")
```

**下午（3小时）：Play Mode 全程测试**
```
测试清单：
□ 站立 → Idle 动画播放
□ 移动 → Run 动画播放，停止回 Idle
□ 攻击 → Attack 动画播放，结束回 Idle
□ 受击 → Hurt 动画播放，结束回 Idle
□ 死亡 → Death 动画播放，不返回
□ 动画切换无卡顿、无穿帧
```

**晚上（1小时）：读代码**
```
读：受击和死亡逻辑
目标：说出"角色死亡后发生了什么"
```

---

#### Day 5（5/13）— 美术方向决定 + 场景整理

**上午（4小时）：确定美术风格（今天必须定，不再改）**
```
三选一：
A. 继续水墨风（接受帧间不一致，快速推进）
B. 去 itch.io 找暗色武侠素材替换场景
C. 全用 SunnyLand，换轻松风格角色

决定后：
→ 把场景整理成统一风格
→ 删除不用的素材，整理 Project 文件夹结构
```

**下午（3小时）：场景基础搭建**
```
□ 用 Tilemap 搭一块平台地形（能站人）
□ 设置正确的碰撞层（Player / Ground / Enemy）
□ 相机跟随角色（Cinemachine 已有，检查参数）
□ 角色能在场景里正常跑跳
```

**晚上（1小时）：手写练习**
```
不看代码，自己从零写一个最简单的状态机：
class StateMachine {
    // Idle / Run / Attack 三个状态
    // 切换方法
}
写完对照项目里的实现，找差异
```

---

#### Day 6（5/14）— 动画细节 + 过渡优化

**上午（4小时）：动画 Transition 参数调优**
```
□ 每个 Transition 的 Transition Duration 设为 0
  （动作游戏需要即时响应，不需要融合过渡）
□ Attack → Idle 的 Exit Time 设为 0.9
  （攻击动画播到90%再结束，留收势感）
□ Hurt → 前状态 Exit Time 设为 1.0
□ 测试每个切换是否自然
```

**下午（3小时）：角色朝向翻转**
```
实现角色面向移动方向：
→ 向右移动：Scale.x = 1（或正值）
→ 向左移动：Scale.x = -1（翻转）
注意：翻转用 Scale 而不是 Rotation，
避免影响子物体碰撞体方向
测试：左右跑动时角色正确翻转
```

**晚上（1小时）：读代码**
```
读：场景管理 / 多场景系统
目标：说出"场景是怎么加载的"
```

---

#### Day 7（5/15）— 周复盘 + 补漏

**上午（4小时）：通关测试 + 修 Bug**
```
从打开游戏开始，走一遍完整流程：
□ 角色出生位置正确
□ 所有动画正常播放
□ 移动手感流畅（速度/加速度合适）
□ 攻击有响应
□ 受击有反馈
□ 记录所有问题，逐一修复
```

**下午（2小时）：整理本周成果**
```
□ 截图：角色在场景里的效果
□ 录制30秒：展示 Idle/Run/Attack 动画
□ 在求职计划里打勾已完成项目
□ 列出下周开始前需要解决的遗留问题
```

**下午（1小时）：学习**
```
看一篇 Unity Animator 原理文章
重点：理解 AnimatorController 的底层逻辑
能回答：Animator 的 Update 和 LateUpdate 有什么区别
```

**晚上（1小时）：手写练习**
```
不看代码，写一个对象池：
class ObjectPool {
    // 初始化（创建N个对象放入池）
    // Get（从池取出）
    // Release（归还到池）
}
```

---

#### 第1周结束检查清单
```
□ 三个动画（Idle/Run/Attack）在游戏里正常运行
□ Animator 状态机连接完整（含Hurt/Death）
□ 角色比例正常，无模糊
□ 角色能在场景里跑跳
□ 读懂了5个脚本
□ 手写了状态机和对象池
□ 美术风格已确定，不再更改
```

### 第2周（5/17 - 5/23）战斗手感
```
□ 攻击顿帧效果（0.05秒暂停）
□ 受击反馈（击退 + 闪烁）
□ 攻击音效 + 受击音效接入
□ 伤害数字弹出 UI
□ 手写一遍状态机（理解原理）
```

### 第3周（5/24 - 5/30）敌人 AI
```
□ 敌人类型1：巡逻 + 近战攻击
□ 敌人类型2：远程或特殊行为
□ 敌人受击 + 死亡动画
□ 敌人掉落物品
□ 手写一遍对象池（理解原理）
```

### 第4周（5/31 - 6/7）关卡设计
```
□ 搭建完整1关卡（有开头、中段、结尾）
□ 障碍物 + 平台布局
□ 关卡节奏（弱→中→强→Boss区域）
□ 检查点 / 复活点
□ 读懂场景管理相关代码
```

### 第5周（6/8 - 6/15）UI + 系统完整
```
□ 主菜单界面
□ HUD（血条、技能CD、金币）
□ 游戏结束 / 胜利界面
□ 暂停菜单
□ 手写一遍观察者模式（事件系统）
```

---

## 第二阶段：Demo1 收尾（6月16日 - 7月1日）

### 第6周（6/16 - 6/22）Polish
```
□ 粒子特效（攻击拖尾、受击爆发、死亡）
□ 相机震动（受击/攻击时）
□ 背景音乐 + 全部音效完善
□ UI 动效（按钮悬停、界面过渡）
□ 整体流程通关测试
```

### 第7周（6/23 - 7/1）打包 + 材料准备
```
□ Windows 可执行文件打包
□ Bug 修复（通关一遍记录所有问题）
□ GitHub 整理（commit记录规范、代码注释）
□ README 编写（截图 + 技术亮点 + 操作说明）
□ 30秒演示视频录制
□ 简历初稿
```

---

## 第三阶段：投简历 + Demo2（7月起）

### 7月上旬：开始投递
```
□ Boss直聘、猎聘、各公司官网同步上线
□ 目标公司：中小游戏厂 + 非游戏Unity岗（XR/工业）
□ 每天复习1个C#原理知识点
□ 每天2道LeetCode简单题
```

### 7月中旬 - 7月底：Demo2 启动
```
□ 方向：小型3D游戏（补足3D技能短板）
□ 找免费3D资源（itch.io / Kenney）
□ 玩家移动 + 相机控制
□ 简单敌人AI
□ 打包 + 接入简历
```

---

## 学习计划（与开发并行）

### 每天固定1小时学习

| 时间段 | 内容 |
|--------|------|
| 现在 - 6月15日 | 读懂自己项目的代码，每天一个脚本 |
| 6月15日 - 6月25日 | C#原理（GC、协程、委托、值/引用类型） |
| 6月25日 - 7月1日 | Unity原理（DrawCall、对象池、生命周期） |
| 7月起 | LeetCode简单题，每天2道 |

### 必须手写的三个系统（6月前完成）
```
□ 简单状态机（Idle/Run/Attack）
□ 对象池（ObjectPool）
□ 观察者模式（EventSystem）
```

---

## 面试准备（7月开始）

### 必须能回答的问题
```
技术类：
□ 你的状态机是怎么设计的？
□ 如何优化DrawCall？
□ GC是什么、怎么避免频繁触发？
□ 协程和线程的区别？
□ 项目里遇到最难的问题是什么？怎么解决的？

项目类：
□ 2分钟介绍你的项目
□ 战斗系统的架构是怎么样的？
□ 如果要加一个新敌人，需要改哪些地方？
```

### 简历核心描述模板
```
"使用Unity开发2D横版动作游戏，实现了：
- 基于状态机的玩家战斗系统（攻击/受击/死亡）
- 具有巡逻/追击/攻击行为的敌人AI
- 程序化生成的商店UI系统
- 事件驱动的音效/特效反馈系统"
```

---

## 目标公司（由易到难）

| 优先级 | 类型 | 代表公司 |
|--------|------|---------|
| 第一批 | 中小游戏厂 | 竹子网络、波克城市、游族 |
| 第一批 | 非游戏Unity | XR公司、工业仿真、汽车HMI |
| 第二批 | 中型游戏厂 | 鹰角、心动、紫龙 |
| 第三批 | 大厂 | 米哈游、莉莉丝、网易 |

---

## 每日时间分配参考

```
上午 4小时：核心开发任务
下午 3小时：次要开发 + 测试
晚上 1小时：学习（读代码/手写练习/刷题）
```

---

> 最后更新：2026-05-09
